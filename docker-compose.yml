services:
  db:
    image: postgres:16-alpine
    container_name: jutti_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: jutti_store
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
